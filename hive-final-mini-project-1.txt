This is a real time dataset of the ineuron technical consultant team. You have to perform hive analysis on this given dataset.
Download Dataset 1 - https://drive.google.com/file/d/1WrG-9qv6atP-W3P_-gYln1hHyFKRKMHP/view

Download Dataset 2 - https://drive.google.com/file/d/1-JIPCZ34dyN6k9CqJa-Y8yxIGq6vTVXU/view

1. Create a schema based on the given dataset
i.AgentLogin_data.
    create external table if not exists agent_login_data
     (
     sl_no int,
     agent string,
     date string,
     login_time string,
     logout_time string,
     duration string
     )
     row format delimited
     fields terminated by ','
     location '/tmp/INeuron_agent_login_data';
     
ii. AgentLogout_data
      create external table if not exists agent_performance
      (
      sl_no int,
      date string,
      agent_name,
      total_chats int,
      average_response string,
      average_resolution string,
      average_rating float,
      total_feedback int
      )
      row format delimited 
      fields terminated by ','
      loactiom '/tmp/INeuron_agent_login_data';
     
2. Dump the data inside the hdfs in the given schema location.
i. hdfs dfs -put /home/cloudera/AgentLogingReport.csv /tmp/INeuron_agent_login_data
ii. hdfs dfs -put /home/cloudera/AgentPerformance.csv /tmp/INeuron_agent_performance_data

3. List of all agents' names. 
     select
     agent
     from agent_login_data;

4. Find out agent average rating.
     select
     agent_name,
     avg(average_rating) as av_rating
     from agent_performance
     group by agent_name;

5. Total working days for each agents 
     select
     agent,
     count(date) as total_working_day
     from agent_login_data
     group by agent
     order by total_working_day;

6. Total query that each agent have taken
     select
     agent_name,
     count(total_chat) as total_query
     from agent_performance
     group by agent_name;
     
7. Total Feedback that each agent have received 
      select
      agent_name,
      total_feedback
      from agent_perfomance;
  
8. Agent name who have average rating between 3.5 to 4 
  
  
9. Agent name who have rating less than 3.5 
     select
     agent_name,
     average_rating
     from agent_performance
     where average_rating < 3.5;

         
10. Agent name who have rating more than 4.5   
     select
     agent_name,
     average_rating
     from agent_performance
     where average_rating > 4.5;

11. How many feedback agents have received more than 4.5 average



  
  
